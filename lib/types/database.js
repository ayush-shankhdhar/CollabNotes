// Database type definitions for reference
// These are JSDoc comments for better IDE support

/**
 * @typedef {string | number | boolean | null | {[key: string]: Json | undefined} | Json[]} Json
 */

/**
 * @typedef {Object} Database
 * @property {Object} public
 * @property {Object} public.Tables
 */

// Export empty object for compatibility
export const Database = {}
